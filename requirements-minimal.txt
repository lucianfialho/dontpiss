# Minimal requirements for older/slower machines
# Works with Python 3.8 - 3.11

# Core dependencies (minimal versions)
opencv-python>=4.5.0
numpy>=1.21.0,<2.0.0

# YOLO (uses PyTorch internally, but will download compatible version)
ultralytics>=8.0.0

# Notifications
plyer>=2.1.0

# Utilities
Pillow>=9.0.0

# Analytics (optional - install only if needed)
# pandas>=1.3.0
# matplotlib>=3.5.0
# seaborn>=0.11.0
