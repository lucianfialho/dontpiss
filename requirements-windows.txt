# Windows-optimized requirements
# Works with Python 3.9-3.11 (recommended)
# For Python 3.12+: some packages may need pre-built wheels

# Core dependencies with pre-built Windows wheels
opencv-python>=4.5.0
numpy>=1.21.0,<2.0.0  # Pin to 1.x for better compatibility

# YOLO (will download PyTorch automatically with compatible version)
ultralytics>=8.0.0

# Notifications
plyer>=2.1.0

# Image processing
Pillow>=9.0.0

# Progress bars
tqdm>=4.65.0

# Configuration
pyyaml>=6.0

# Analytics (optional but useful)
pandas>=1.3.0,<2.0.0  # Pin to 1.x for stability
matplotlib>=3.5.0
seaborn>=0.11.0
