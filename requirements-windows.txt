# Windows-optimized requirements
# Try to use pre-built wheels when available

# Core dependencies - let pip find best version
opencv-python
numpy  # Let pip resolve best available version
ultralytics  # Includes PyTorch with compatible versions

# Utilities
plyer
Pillow
tqdm
pyyaml

# Analytics (optional - can skip if installation fails)
# pandas
# matplotlib
# seaborn
